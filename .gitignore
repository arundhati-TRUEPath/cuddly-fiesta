# Never commit secrets. All API keys live in Netlify's environment
# variables, never in this repository.
.env
.env.*
!.env.example
*.key

# Netlify local dev
.netlify/

# Dependencies (this project has none, but just in case)
node_modules/

# OS noise
.DS_Store
Thumbs.db
